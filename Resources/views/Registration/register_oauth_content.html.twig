<div class="row-fluid">
    <div class="span12">
    <form action="{{ path('rz_o_auth_registration_complete_registration', {'key': key}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
        <div class="row-fluid">
            {{ form_widget(form) }}
            <div class="form-actions">
                <input class="btn btn-inverse" type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
            </div>
        </div>
    </form>
    </div>
    {#<div class="span6">#}
        {#{% if userInformation.profilePicture is not empty %}#}
            {#<img src="{{ userInformation.profilePicture }}" />#}
        {#{% endif %}#}
    {#</div>#}
</div>
